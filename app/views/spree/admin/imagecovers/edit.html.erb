<%= render :partial => 'spree/admin/shared/box_tabs', :locals => {:current => 'Images'}%>

<% content_for :page_actions do %>
  <li><%= button_link_to Spree.t(:back_to_images_list), admin_box_imagecovers_url(@box), :icon => 'arrow-left' %></li>
<% end %>

<%= form_for [@box, @imagecover], url: admin_box_imagecover_url, :html => { :multipart => true } do |f| %>
  <fieldset data-hook="edit_image">
    <legend align="center"><%= @imagecover.attachment_file_name%></legend>
    <div data-hook="thumbnail" class="field alpha three columns align-center">
      <%= f.label Spree.t(:thumbnail) %><br>
      <%= link_to image_tag(@imagecover.attachment.url(:mini)), @imagecover.attachment.url(:box) %>
    </div>
    <div class="nine columns omega">
      <%= render :partial => 'form', :locals => { :f => f } %>  
    </div>
    <div class="clear"></div>
    <div class="form-buttons filter-actions actions" data-hook="buttons">
      <%= button Spree.t('actions.update'), 'refresh' %>
      <span class="or"><%= Spree.t(:or) %></span>
      <%= link_to Spree.t('actions.cancel'), admin_box_imagecovers_url(@box), :id => 'cancel_link', :class => 'button remove' %>
    </div>
  </fieldset>
<% end %>
