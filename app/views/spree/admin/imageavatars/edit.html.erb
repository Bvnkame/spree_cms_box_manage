<%= render :partial => 'spree/admin/shared/box_sub_menu'%>
<%= render :partial => 'spree/admin/shared/expert_tabs', :locals => {:current => 'Images'}%>

<% content_for :page_actions do %>
  <li><%= button_link_to Spree.t(:back_to_images_list), admin_expert_imageavatars_url(@expert), :icon => 'arrow-left' %></li>
<% end %>

<%= form_for [@expert, @imageavatar], url: admin_expert_imageavatar_url, :html => { :multipart => true } do |f| %>
  <fieldset data-hook="edit_image">
    <legend align="center"><%= @imageavatar.attachment_file_name%></legend>
    <div data-hook="thumbnail" class="field alpha three columns align-center">
      <%= f.label Spree.t(:thumbnail) %><br>
      <%= link_to image_tag(@imageavatar.attachment.url(:mini)), @imageavatar.attachment.url(:expert) %>
    </div>
    <div class="nine columns omega">
      <%= render :partial => 'form', :locals => { :f => f } %>  
    </div>
    <div class="clear"></div>
    <div class="form-buttons filter-actions actions" data-hook="buttons">
      <%= button Spree.t('actions.update'), 'refresh' %>
      <span class="or"><%= Spree.t(:or) %></span>
      <%= link_to Spree.t('actions.cancel'), admin_expert_imageavatars_url(@expert), :id => 'cancel_link', :class => 'button remove' %>
    </div>
  </fieldset>
<% end %>
